"use client";import{useState}from"react";import{useRouter}from"next/navigation";import styles from"@/app/homes/[homeId]/items/items.module.css";
type Expense={id:string;homeItemId:string|null;repairId:string|null;category:string;title:string;amount:{toString():string};expenseDate:Date;paymentMethod:string|null;memo:string|null};const labels={PURCHASE:"구매",REPAIR:"수리",MAINTENANCE:"유지관리",MANAGEMENT:"관리비",UTILITY:"공과금",CONTRACT:"계약관련",INSURANCE:"보험",TAX:"세금",OTHER:"기타"};
export function ExpenseForm({homeId,items,repairs,expense}:{homeId:string;items:{id:string;name:string}[];repairs:{id:string;title:string;homeItemId:string|null}[];expense?:Expense}){const router=useRouter();const[error,setError]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const body=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch(`/api/homes/${homeId}/expenses${expense?`/${expense.id}`:""}`,{method:expense?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await response.json();if(!response.ok){setError(data.message);return}router.push(`/homes/${homeId}/expenses`);router.refresh()}const date=(value:Date)=>new Date(value).toISOString().slice(0,10);return <form className={styles.form} onSubmit={submit}><label htmlFor="title">비용명 *</label><input id="title" name="title" defaultValue={expense?.title} maxLength={120} required/><div className={styles.two}><div><label htmlFor="category">카테고리 *</label><select id="category" name="category" defaultValue={expense?.category??"OTHER"}>{Object.entries(labels).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select></div><div><label htmlFor="amount">금액 *</label><input id="amount" name="amount" type="number" inputMode="numeric" min="0" defaultValue={expense?.amount.toString()} required/></div></div><label htmlFor="expenseDate">날짜 *</label><input id="expenseDate" name="expenseDate" type="date" defaultValue={date(expense?.expenseDate??new Date())} required/><label htmlFor="homeItemId">관련 물건</label><select id="homeItemId" name="homeItemId" defaultValue={expense?.homeItemId??""}><option value="">연결하지 않음</option>{items.map(item=><option value={item.id} key={item.id}>{item.name}</option>)}</select><label htmlFor="repairId">관련 수리·점검</label><select id="repairId" name="repairId" defaultValue={expense?.repairId??""}><option value="">연결하지 않음</option>{repairs.map(repair=><option value={repair.id} key={repair.id}>{repair.title}</option>)}</select><label htmlFor="paymentMethod">결제수단</label><input id="paymentMethod" name="paymentMethod" defaultValue={expense?.paymentMethod??""} maxLength={80}/><label htmlFor="memo">개인 메모</label><textarea id="memo" name="memo" defaultValue={expense?.memo??""} maxLength={2000}/><p className={styles.meta}>금액, 결제수단, 메모는 커뮤니티나 장터에 공개되지 않습니다.</p>{error&&<p className={styles.error} role="alert">{error}</p>}<button className={styles.primary}>{expense?"수정 완료":"비용 등록"}</button></form>}
