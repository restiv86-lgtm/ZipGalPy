"use client";import{useState}from"react";import{useRouter}from"next/navigation";import styles from"@/app/homes/[homeId]/items/items.module.css";type S={id:string;homeItemId:string|null;repairId:string|null;title:string;scheduledAt:Date;type:string;completed:boolean;memo:string|null};const types={INSPECTION:"점검",WARRANTY:"보증",REPAIR:"수리",CONTRACT:"계약",PAYMENT:"납부",OTHER:"기타"};const dt=(v:Date)=>new Date(new Date(v).getTime()-new Date(v).getTimezoneOffset()*60000).toISOString().slice(0,16);
export function ScheduleForm({homeId,items,schedule}:{homeId:string;items:{id:string;name:string}[];schedule?:S}){const router=useRouter();const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),body=Object.fromEntries(f);Object.assign(body,{completed:f.get("completed")==="on",repairId:schedule?.repairId??""});const r=await fetch(`/api/homes/${homeId}/schedules${schedule?`/${schedule.id}`:""}`,{method:schedule?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(!r.ok){setError(d.message);return}router.push(`/homes/${homeId}/schedules`);router.refresh()}return <form className={styles.form} onSubmit={submit}><label htmlFor="homeItemId">연결 물건</label><select id="homeItemId" name="homeItemId" defaultValue={schedule?.homeItemId??""}><option value="">집 전체</option>{items.map(i=><option key={i.id} value={i.id}>{i.name}</option>)}</select><div className={styles.two}><div><label htmlFor="type">유형</label><select id="type" name="type" defaultValue={schedule?.type??"INSPECTION"}>{Object.entries(types).map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></div><div><label htmlFor="scheduledAt">일정 일시</label><input id="scheduledAt" name="scheduledAt" type="datetime-local" defaultValue={dt(schedule?.scheduledAt??new Date())} required/></div></div><label htmlFor="title">제목</label><input id="title" name="title" defaultValue={schedule?.title} required/><label htmlFor="memo">메모</label><textarea id="memo" name="memo" defaultValue={schedule?.memo??""}/><label><input name="completed" type="checkbox" defaultChecked={schedule?.completed}/> 일정 완료</label>{error&&<p className={styles.error} role="alert">{error}</p>}<button className={styles.primary}>{schedule?"수정 완료":"일정 등록"}</button></form>}
