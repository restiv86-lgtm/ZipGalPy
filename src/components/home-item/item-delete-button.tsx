"use client";import{useRouter}from"next/navigation";import styles from"@/app/homes/[homeId]/items/items.module.css";export function ItemDeleteButton({homeId,itemId}:{homeId:string;itemId:string}){const router=useRouter();async function remove(){if(!confirm("이 물건을 삭제할까요? 장터 게시글은 삭제되지 않습니다."))return;const r=await fetch(`/api/homes/${homeId}/items/${itemId}`,{method:"DELETE"});if(r.ok){router.push(`/homes/${homeId}/items`);router.refresh()}}return <button className={styles.danger} onClick={remove}>삭제</button>}
