"use client";
import { useState } from "react";
import styles from "@/app/apartments/[id]/community/community.module.css";
export function ReportButton({apartmentId,targetType,targetId}:{apartmentId:string;targetType:"POST"|"COMMENT";targetId:string}){const [message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const f=new FormData(event.currentTarget);const response=await fetch(`/api/apartments/${apartmentId}/community/reports`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({targetType,targetId,reason:f.get("reason"),detail:f.get("detail")})});const data=await response.json();setMessage(data.message)}return <details className={styles.report}><summary>신고</summary><form className={styles.reportBox} onSubmit={submit}><strong>신고 사유</strong><select name="reason"><option value="SPAM_AD">스팸/광고</option><option value="ABUSE">욕설/비방</option><option value="INAPPROPRIATE">부적절한 콘텐츠</option><option value="SUSPECTED_FRAUD">사기 의심</option><option value="OTHER">기타</option></select><textarea name="detail" maxLength={500} placeholder="상세 내용 (선택)"/><button className={styles.secondary}>신고 접수</button>{message&&<small role="status">{message}</small>}</form></details>}
