import{Prisma}from"@/generated/prisma/client";import{getPrisma}from"@/lib/prisma";import type{ContractInput}from"./validation";const data=(input:ContractInput)=>({...input,reminderDays:[...new Set(input.reminderDays)].sort((a,b)=>b-a),amount:input.amount==null?null:new Prisma.Decimal(input.amount)});
export async function listContracts(userId:string,homeId:string){const home=await getPrisma().home.findFirst({where:{id:homeId,userId},select:{id:true,name:true}});if(!home)return null;return{home,contracts:await getPrisma().homeContract.findMany({where:{homeId},orderBy:[{status:"asc"},{endDate:"asc"},{createdAt:"desc"}]})}}
export async function createContract(userId:string,homeId:string,input:ContractInput){if(!await getPrisma().home.findFirst({where:{id:homeId,userId},select:{id:true}}))return null;return getPrisma().homeContract.create({data:{...data(input),homeId}})}
export function getOwnedContract(userId:string,homeId:string,id:string){return getPrisma().homeContract.findFirst({where:{id,homeId,home:{userId}}})}
export async function updateContract(userId:string,homeId:string,id:string,input:ContractInput){const result=await getPrisma().homeContract.updateMany({where:{id,homeId,home:{userId}},data:data(input)});return result.count?getOwnedContract(userId,homeId,id):null}
export async function deleteContract(userId:string,homeId:string,id:string){return(await getPrisma().homeContract.deleteMany({where:{id,homeId,home:{userId}}})).count===1}
export function derivedContractStatus(contract:{status:string;endDate:Date|null},now=new Date()){if(contract.status==="TERMINATED")return"TERMINATED";if(!contract.endDate)return"ACTIVE";const days=Math.ceil((contract.endDate.getTime()-now.getTime())/86400000);return days<0?"EXPIRED":days<=90?"EXPIRING":"ACTIVE"}
