import{z}from"zod";const text=(max:number)=>z.string().trim().max(max).optional().transform(value=>value||null),date=z.preprocess(value=>value===""||value==null?null:value,z.coerce.date().nullable()),amount=z.preprocess(value=>value===""||value==null?null:value,z.coerce.number().int().min(0).max(99_999_999_999_999).nullable());
export const contractSchema=z.object({type:z.enum(["LEASE","RENTAL","INSURANCE","INTERNET","SECURITY","MAINTENANCE","SUBSCRIPTION","OTHER"]),title:z.string().trim().min(1).max(120),companyName:text(120),contractNumber:text(120),startDate:date,endDate:date,amount,memo:text(2000),status:z.enum(["ACTIVE","TERMINATED"]).default("ACTIVE"),reminderDays:z.array(z.number().int().min(1).max(365)).max(8).default([90,60,30,7])}).refine(value=>!value.startDate||!value.endDate||value.endDate>=value.startDate,{message:"종료일은 시작일 이후여야 합니다.",path:["endDate"]});export type ContractInput=z.infer<typeof contractSchema>;
