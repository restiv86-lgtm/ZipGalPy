import{getServerSession}from"next-auth";import Link from"next/link";import{authOptions}from"@/lib/auth/options";import{selectOwnedHomeId}from"@/lib/home/navigation";import{listHomes}from"@/lib/home/service";import{getHomeAlerts}from"@/lib/notification/service";import styles from"./dashboard.module.css";
export default async function DashboardLayout({children}:{children:React.ReactNode}){const session=await getServerSession(authOptions);if(!session?.user?.id)return children;const[homes,alerts]=await Promise.all([listHomes(session.user.id),getHomeAlerts(session.user.id)]);const selectedHomeId=selectOwnedHomeId(homes);return <>{children}<aside className={styles.page} aria-labelledby="alerts-title"><div className={`${styles.container} ${styles.content}`}><section><div className={styles.sectionHeading}><div><p className={styles.eyebrow}>ZIPGALPY REMINDERS</p><h2 id="alerts-title">집갈피가 챙겨드릴게요</h2></div></div>{alerts.length?<div className={styles.homeGrid}>{alerts.map(alert=><article className={styles.homeCard} key={alert.id}><div><span>{alert.importance==="URGENT"?"가까운 알림":alert.importance==="SOON"?"곧 다가와요":"미리 확인해요"} · {alert.homeName}</span><h3>{alert.title}</h3><p>{alert.date.toLocaleDateString("ko-KR")}</p></div><Link href={alert.href}>확인하기 →</Link></article>)}</div>:<div className={styles.empty}><span aria-hidden="true">✓</span><h3>현재 확인할 알림이 없습니다</h3><p>일정, 계약, 보증기간과 다음 점검일을 집갈피가 함께 확인합니다.</p></div>}</section><section><div className={styles.sectionHeading}><div><p className={styles.eyebrow}>CONTRACTS</p><h2>계약관리</h2></div></div><div className={styles.menuGrid}><Link href={homes.length===0?"/dashboard/contracts":selectedHomeId?`/homes/${selectedHomeId}/contracts`:"/dashboard/contracts"}><span>📝</span><strong>계약</strong><small>계약과 만료일 관리</small></Link></div></section></div></aside></>}
