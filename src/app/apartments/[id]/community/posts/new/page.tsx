import { getServerSession } from "next-auth";import Link from "next/link";import { notFound,redirect } from "next/navigation";import { CommunityForm } from "@/components/community/community-form";import { authOptions } from "@/lib/auth/options";import { getMembership } from "@/lib/community/service";import styles from "../../community.module.css";
export default async function NewPostPage({params}:PageProps<"/apartments/[id]/community/posts/new">){const s=await getServerSession(authOptions);if(!s?.user?.id)redirect("/login");const {id}=await params;const member=await getMembership(s.user.id,id);if(!member)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/apartments/${id}/community/posts`}>← 게시판</Link><div className={styles.heading}><div><p className={styles.eyebrow}>{member.apartment.name}</p><h1>새 게시글</h1></div></div><CommunityForm apartmentId={id}/></div></main>}
