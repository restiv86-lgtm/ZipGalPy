import { getServerSession } from "next-auth";import Link from "next/link";import { notFound,redirect } from "next/navigation";import { CommunityForm } from "@/components/community/community-form";import { authOptions } from "@/lib/auth/options";import { getPost } from "@/lib/community/service";import styles from "../../../community.module.css";
export default async function EditPost({params}:PageProps<"/apartments/[id]/community/posts/[postId]/edit">){const s=await getServerSession(authOptions);if(!s?.user?.id)redirect("/login");const {id,postId}=await params;const post=await getPost(s.user.id,id,postId);if(!post||post.authorMember.userId!==s.user.id)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/apartments/${id}/community/posts/${postId}`}>← 게시글</Link><h1>게시글 수정</h1><CommunityForm apartmentId={id} post={{id:post.id,category:post.category,title:post.title,content:post.content}}/></div></main>}
