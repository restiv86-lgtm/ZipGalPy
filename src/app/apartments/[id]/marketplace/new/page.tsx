import { getServerSession } from "next-auth";import Link from "next/link";import { notFound,redirect } from "next/navigation";import { MarketplaceForm } from "@/components/community/marketplace-form";import { authOptions } from "@/lib/auth/options";import { getMembership } from "@/lib/community/service";import styles from "../../community/community.module.css";
export default async function NewMarketplace({params}:PageProps<"/apartments/[id]/marketplace/new">){const s=await getServerSession(authOptions);if(!s?.user?.id)redirect("/login");const {id}=await params;const member=await getMembership(s.user.id,id);if(!member)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/apartments/${id}/marketplace`}>← 장터</Link><p className={styles.eyebrow}>{member.apartment.name}</p><h1>나눔/중고거래 등록</h1><MarketplaceForm apartmentId={id}/></div></main>}
