import{getServerSession}from"next-auth";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{ContractDeleteButton}from"@/components/home-contract/contract-delete-button";import{ContractForm}from"@/components/home-contract/contract-form";import{authOptions}from"@/lib/auth/options";import{getOwnedContract}from"@/lib/home-contract/service";import styles from"../../../items/items.module.css";export default async function EditContract({params}:PageProps<"/homes/[homeId]/contracts/[contractId]/edit">){const session=await getServerSession(authOptions);if(!session?.user?.id)redirect("/login");const{homeId,contractId}=await params,contract=await getOwnedContract(session.user.id,homeId,contractId);if(!contract)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/homes/${homeId}/contracts`}>← 계약 목록</Link><div className={styles.heading}><h1>계약 수정</h1><ContractDeleteButton homeId={homeId} contractId={contractId}/></div><ContractForm homeId={homeId} contract={contract}/></div></main>}
