import{getServerSession}from"next-auth";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{ContractForm}from"@/components/home-contract/contract-form";import{authOptions}from"@/lib/auth/options";import{listContracts}from"@/lib/home-contract/service";import styles from"../../items/items.module.css";export default async function NewContract({params}:PageProps<"/homes/[homeId]/contracts/new">){const session=await getServerSession(authOptions);if(!session?.user?.id)redirect("/login");const{homeId}=await params,data=await listContracts(session.user.id,homeId);if(!data)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/homes/${homeId}/contracts`}>← 계약 목록</Link><div className={styles.heading}><div><p className={styles.eyebrow}>{data.home.name}</p><h1>계약 등록</h1></div></div><ContractForm homeId={homeId}/></div></main>}
