import{getServerSession}from"next-auth";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{HomeItemForm}from"@/components/home-item/home-item-form";import{authOptions}from"@/lib/auth/options";import{getOwnedHome}from"@/lib/home/service";import styles from"../items.module.css";export default async function NewItem({params}:PageProps<"/homes/[homeId]/items/new">){const s=await getServerSession(authOptions);if(!s?.user?.id)redirect("/login");const{homeId}=await params;const home=await getOwnedHome(s.user.id,homeId);if(!home)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/homes/${homeId}/items`}>← 물건 목록</Link><div className={styles.heading}><div><p className={styles.eyebrow}>{home.name}</p><h1>물건 등록</h1></div></div><HomeItemForm homeId={homeId}/></div></main>}
