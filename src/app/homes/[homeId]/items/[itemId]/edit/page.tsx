import{getServerSession}from"next-auth";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{HomeItemForm}from"@/components/home-item/home-item-form";import{authOptions}from"@/lib/auth/options";import{getOwnedItem}from"@/lib/home-item/service";import styles from"../../items.module.css";export default async function EditItem({params}:PageProps<"/homes/[homeId]/items/[itemId]/edit">){const s=await getServerSession(authOptions);if(!s?.user?.id)redirect("/login");const{homeId,itemId}=await params;const item=await getOwnedItem(s.user.id,homeId,itemId);if(!item)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/homes/${homeId}/items/${itemId}`}>← 물건 상세</Link><h1>물건 수정</h1><HomeItemForm homeId={homeId} item={item}/></div></main>}
