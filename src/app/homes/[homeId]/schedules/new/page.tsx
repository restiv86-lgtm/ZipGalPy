import { getServerSession } from "next-auth";import Link from "next/link";import{notFound,redirect}from"next/navigation";import{ScheduleForm}from"@/components/home-management/schedule-form";import{authOptions}from"@/lib/auth/options";import{listSchedules}from"@/lib/home-management/service";import styles from"../../items/items.module.css";
export default async function NewSchedule({params}:PageProps<"/homes/[homeId]/schedules/new">){const session=await getServerSession(authOptions);if(!session?.user?.id)redirect("/login");const{homeId}=await params;const data=await listSchedules(session.user.id,homeId);if(!data)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/homes/${homeId}/schedules`}>← 일정 목록</Link><div className={styles.heading}><div><p className={styles.eyebrow}>{data.home.name}</p><h1>일정 등록</h1></div></div><ScheduleForm homeId={homeId} items={data.items}/></div></main>}
