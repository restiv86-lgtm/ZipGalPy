import{getServerSession}from"next-auth";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{authOptions}from"@/lib/auth/options";import{listExpenses}from"@/lib/home-expense/service";import styles from"../items/items.module.css";const labels={PURCHASE:"구매",REPAIR:"수리",MAINTENANCE:"유지관리",MANAGEMENT:"관리비",UTILITY:"공과금",CONTRACT:"계약관련",INSURANCE:"보험",TAX:"세금",OTHER:"기타"};
export default async function ExpensesPage({params,searchParams}:PageProps<"/homes/[homeId]/expenses">){const session=await getServerSession(authOptions);if(!session?.user?.id)redirect("/login");const{homeId}=await params;const query=await searchParams;const period=query.period==="year"||query.period==="all"?query.period:"month";const data=await listExpenses(session.user.id,homeId,period);if(!data)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href="/dashboard">← Dashboard</Link><div className={styles.heading}><div><p className={styles.eyebrow}>{data.home.name}</p><h1>비용관리</h1></div><Link className={styles.primary} href={`/homes/${homeId}/expenses/new`}>+ 비용 등록</Link></div><nav className={styles.actions} aria-label="비용 기간"><Link className={period==="month"?styles.primary:styles.secondary} href="?period=month">이번 달</Link><Link className={period==="year"?styles.primary:styles.secondary} href="?period=year">올해</Link><Link className={period==="all"?styles.primary:styles.secondary} href="?period=all">전체</Link></nav><section className={styles.detail}><p className={styles.meta}>선택 기간 총 지출</p><h2>{data.total.toLocaleString("ko-KR")}원</h2><div className={styles.grid}>{data.summary.map(row=><div className={styles.card} key={row.category}><span>{labels[row.category]}</span><strong>{Number(row.amount).toLocaleString("ko-KR")}원</strong></div>)}</div></section>{data.expenses.length?<div className={styles.grid}>{data.expenses.map(expense=><Link className={styles.card} href={`/homes/${homeId}/expenses/${expense.id}/edit`} key={expense.id}><span className={styles.badge}>{labels[expense.category]}</span><h2>{expense.title}</h2><span className={styles.meta}>{expense.expenseDate.toLocaleDateString("ko-KR")}{expense.homeItem?` · ${expense.homeItem.name}`:""}</span><strong>{Number(expense.amount).toLocaleString("ko-KR")}원</strong></Link>)}</div>:<div className={styles.empty}>선택한 기간의 비용 기록이 없습니다.</div>}</div></main>}
