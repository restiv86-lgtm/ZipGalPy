import{getServerSession}from"next-auth";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{ExpenseForm}from"@/components/home-expense/expense-form";import{authOptions}from"@/lib/auth/options";import{listExpenses}from"@/lib/home-expense/service";import styles from"../../items/items.module.css";export default async function NewExpense({params}:PageProps<"/homes/[homeId]/expenses/new">){const session=await getServerSession(authOptions);if(!session?.user?.id)redirect("/login");const{homeId}=await params;const data=await listExpenses(session.user.id,homeId,"all");if(!data)notFound();return <main className={styles.page}><div className={styles.shell}><Link className={styles.back} href={`/homes/${homeId}/expenses`}>← 비용 목록</Link><div className={styles.heading}><div><p className={styles.eyebrow}>{data.home.name}</p><h1>비용 등록</h1></div></div><ExpenseForm homeId={homeId} items={data.items} repairs={data.repairs}/></div></main>}
